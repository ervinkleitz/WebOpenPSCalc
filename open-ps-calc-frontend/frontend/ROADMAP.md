# Roadmap / port status

The authoritative port-status document lives at
[`open-ps-calc-backend/backend/ROADMAP.md`](../../open-ps-calc-backend/backend/ROADMAP.md).

This frontend is a React/TypeScript single-page app built from scratch (not a
port of the original Qt GUI), so there is no file-for-file translation table to
maintain here. The backend engine is the ported artefact; all port-status
tracking is in the backend ROADMAP above.
